# 多租户注册制落地方案

## 1. 落地目标

当前工程已经具备公共收益、账单缴费、支出审批、监管驾驶舱、业主端等演示能力，但现有模型主要是：

- 用户表 `app_user` 直接保存 `role`
- 用户直接绑定单个 `community_id`
- 小区、物业、政府、银行之间还没有独立的多对多关系表

正式落地时，不能继续把 `community_id` 当作唯一隔离边界。应升级为：

> 机构为租户，小区为协作空间，关系表确定主体身份，授权表确定数据边界，注册审核流确定准入。

一期目标不是重做全部业务，而是在现有系统上补齐四个基础能力：

1. 机构注册和审核
2. 小区备案和多方绑定
3. 住户实名注册和房屋绑定
4. 基于租户、小区、数据范围的权限隔离

## 2. 一期范围

### 必须做

1. 平台机构租户
   - 政府机构
   - 物业公司
   - 银行机构
   - 平台运营方

2. 小区主数据
   - 小区由平台统一建档
   - 物业、政府、银行通过关系表绑定小区
   - 支持同一小区多机构协作

3. 注册审核
   - 物业注册
   - 银行注册
   - 政府账号开通
   - 小区备案
   - 住户实名绑定房屋

4. 权限隔离
   - 用户可加入多个租户
   - 用户在不同租户下有不同角色
   - 访问数据时同时校验租户、小区、角色、数据范围

5. 业务闭环
   - 物业生成账单
   - 住户缴费
   - 银行回调/流水同步
   - 政府查看监管数据

### 暂缓二期

1. 复杂工作流引擎
2. 多银行真实直连
3. 信用评价体系
4. 电子票据深度对接
5. 数据交换平台
6. 大屏专题分析

## 3. 主体关系设计

### 核心关系

```text
政府机构 N : N 小区
物业公司 N : N 小区
银行机构 N : N 小区
住户 N : N 房屋
房屋 N : 1 小区
用户 N : N 租户
租户 N : N 小区
```

### 小区不是租户私有数据

小区是平台公共主数据，不属于某一家物业。物业只是通过服务关系绑定小区。

这样可以支持：

- 一个物业公司管理多个小区
- 一个小区更换物业
- 一个小区同时受社区、街道、住建部门监管
- 一个小区绑定多个银行能力
- 一个住户绑定多个房屋

## 4. 数据库落地

建议新增 Flyway 脚本：

```text
backend/src/main/resources/db/migration/V6__tenant_registration.sql
backend/src/main/resources/db/migration/V7__tenant_community_authorization.sql
backend/src/main/resources/db/migration/V8__resident_registration.sql
```

### 4.1 租户机构表

```sql
create table tenant (
  id bigint primary key auto_increment,
  tenant_code varchar(64) not null unique,
  tenant_name varchar(128) not null,
  tenant_type varchar(32) not null,
  parent_id bigint,
  unified_credit_code varchar(64),
  contact_name varchar(64),
  contact_phone varchar(32),
  status varchar(32) not null,
  certification_status varchar(32) not null,
  created_at datetime not null,
  updated_at datetime not null
);
```

`tenant_type` 建议枚举：

```text
PLATFORM
GOVERNMENT
PROPERTY
BANK
COMMITTEE
```

### 4.2 用户与租户关系

```sql
create table user_tenant_relation (
  id bigint primary key auto_increment,
  user_id bigint not null,
  tenant_id bigint not null,
  role_code varchar(64) not null,
  status varchar(32) not null,
  is_default tinyint not null default 0,
  created_at datetime not null,
  unique key uk_user_tenant_role(user_id, tenant_id, role_code)
);
```

现有 `app_user.role` 和 `app_user.community_id` 保留兼容，但新权限判断逐步迁移到 `user_tenant_relation`。

### 4.3 小区与租户关系

```sql
create table tenant_community_relation (
  id bigint primary key auto_increment,
  tenant_id bigint not null,
  community_id bigint not null,
  relation_type varchar(32) not null,
  status varchar(32) not null,
  start_date date,
  end_date date,
  created_at datetime not null,
  unique key uk_tenant_community_relation(tenant_id, community_id, relation_type)
);
```

`relation_type` 建议枚举：

```text
JURISDICTION       政府管辖
SUPERVISION        政府监管
PROPERTY_SERVICE   物业服务
BANK_COLLECTION    银行代收
BANK_SUPERVISION   银行监管账户
COMMITTEE_GOVERN   业委会治理
```

### 4.4 数据授权表

```sql
create table data_authorization (
  id bigint primary key auto_increment,
  grantor_tenant_id bigint not null,
  grantee_tenant_id bigint not null,
  community_id bigint not null,
  data_scope varchar(64) not null,
  permission_code varchar(32) not null,
  status varchar(32) not null,
  effective_from datetime not null,
  effective_to datetime,
  created_at datetime not null
);
```

`data_scope` 建议枚举：

```text
COMMUNITY_PROFILE
HOUSE
RESIDENT
BILLING
PAYMENT
BANK_FLOW
PUBLIC_REVENUE
EXPENSE
COMPLAINT
REPAIR
VOTE
AUDIT
```

`permission_code` 建议枚举：

```text
READ
WRITE
APPROVE
EXPORT
MANAGE
```

### 4.5 注册申请表

```sql
create table registration_application (
  id bigint primary key auto_increment,
  application_no varchar(64) not null unique,
  applicant_type varchar(32) not null,
  applicant_name varchar(128) not null,
  applicant_phone varchar(32) not null,
  target_type varchar(32) not null,
  target_id bigint,
  payload_json text not null,
  status varchar(32) not null,
  submitted_at datetime not null,
  reviewed_by varchar(64),
  reviewed_at datetime,
  review_comment varchar(500)
);
```

`target_type`：

```text
TENANT
COMMUNITY
RESIDENT_HOUSE
BANK_SERVICE
```

### 4.6 住户房屋关系

```sql
create table resident_house_relation (
  id bigint primary key auto_increment,
  user_id bigint not null,
  house_id bigint not null,
  resident_type varchar(32) not null,
  verification_status varchar(32) not null,
  is_primary tinyint not null default 0,
  start_date date,
  end_date date,
  created_at datetime not null,
  unique key uk_user_house_type(user_id, house_id, resident_type)
);
```

`resident_type`：

```text
OWNER
TENANT
FAMILY_MEMBER
AGENT
COMPANY_USER
```

## 5. 后端接口落地

建议新增控制器：

```text
TenantController
RegistrationController
AuthorizationController
ResidentRegistrationController
```

### 5.1 机构注册

```text
POST /api/registrations/tenants
GET  /api/registrations/pending
POST /api/registrations/{id}/approve
POST /api/registrations/{id}/reject
GET  /api/tenants
GET  /api/tenants/{id}
```

提交物业注册示例：

```json
{
  "tenantType": "PROPERTY",
  "tenantName": "武汉鼎盛阳光物业服务有限公司",
  "unifiedCreditCode": "91420100XXXXXXXXXX",
  "contactName": "李经理",
  "contactPhone": "13800000000",
  "licenseFileId": 1001
}
```

审核通过后系统动作：

1. 创建 `tenant`
2. 创建管理员用户或绑定已有用户
3. 写入 `user_tenant_relation`
4. 记录 `audit_log`

### 5.2 小区备案

```text
POST /api/registrations/communities
POST /api/communities/{communityId}/relations
GET  /api/communities/{communityId}/relations
POST /api/communities/{communityId}/relations/{relationId}/disable
```

绑定物业示例：

```json
{
  "tenantId": 20,
  "relationType": "PROPERTY_SERVICE",
  "startDate": "2026-06-01"
}
```

### 5.3 数据授权

```text
POST /api/authorizations
GET  /api/authorizations?communityId=1
POST /api/authorizations/{id}/revoke
```

授权银行读取流水和对账：

```json
{
  "grantorTenantId": 10,
  "granteeTenantId": 30,
  "communityId": 1,
  "dataScopes": ["PAYMENT", "BANK_FLOW"],
  "permissions": ["READ", "WRITE"]
}
```

### 5.4 住户注册和房屋绑定

```text
POST /api/resident/register
POST /api/resident/house-bindings
GET  /api/resident/house-bindings/me
POST /api/resident/house-bindings/{id}/approve
POST /api/resident/house-bindings/{id}/reject
```

## 6. 权限判断落地

当前代码里主要是：

```text
role + community_id
```

一期升级为：

```text
user_id
+ active_tenant_id
+ role_code
+ community_id
+ relation_type
+ data_scope
+ action
```

后端建议新增 `AccessControlService`：

```text
boolean hasCommunityAccess(user, tenantId, communityId)
boolean hasDataPermission(user, tenantId, communityId, dataScope, action)
List<Long> allowedCommunityIds(user, tenantId, dataScope, action)
```

迁移策略：

1. 第一步：保留老 token 字段，新增 `tenantId`
2. 第二步：管理端登录后选择当前机构
3. 第三步：所有列表接口从 `community_id` 单点过滤改为 `allowedCommunityIds`
4. 第四步：下线旧的 `app_user.role` 直接判断

## 7. 前端页面落地

### 管理端新增菜单

```text
平台注册
  - 机构审核
  - 小区备案审核
  - 住户绑定审核

租户管理
  - 机构列表
  - 机构用户
  - 机构角色

小区关系
  - 小区关联机构
  - 物业服务关系
  - 政府管辖关系
  - 银行服务关系

数据授权
  - 授权列表
  - 新增授权
  - 授权撤销
```

### 住户端新增页面

```text
注册登录
实名认证
绑定房屋
绑定审核进度
多房屋切换
```

### 登录流程调整

用户登录后如果属于多个机构：

1. 先登录账号
2. 返回可用机构列表
3. 用户选择当前机构
4. 后端签发包含 `tenantId` 的 token
5. 前端按当前机构加载菜单和数据

## 8. 一期开发排期

### 第 1 周：数据模型和兼容迁移

交付：

1. 新增租户、关系、授权、注册申请表
2. 初始化演示数据迁移到租户模型
3. 登录接口返回当前用户可用租户
4. 保持旧演示功能可用

验收：

```text
admin 能看全部
gov 能看政府授权小区
property 只能看服务小区
bank 只能看授权资金和流水
owner 只能看自己房屋
```

### 第 2 周：注册审核闭环

交付：

1. 物业注册申请
2. 银行注册申请
3. 小区备案申请
4. 住户房屋绑定申请
5. 审核通过后自动创建关系

验收：

```text
物业提交注册 -> 平台审核 -> 物业管理员登录
物业申报小区 -> 政府/平台审核 -> 小区生效
住户提交房屋绑定 -> 物业审核 -> 业主端看到账单
```

### 第 3 周：权限中心替换

交付：

1. `AccessControlService`
2. 列表接口按授权小区过滤
3. 详情接口防越权
4. 导出接口加审计
5. 管理端按角色和数据范围显示菜单

验收：

```text
跨小区访问返回 403
未授权数据范围返回 403
导出审计日志可追溯到用户、租户、小区、数据范围
```

### 第 4 周：银行与缴费闭环

交付：

1. 小区银行服务配置
2. 物业账单生成
3. 住户缴费
4. 银行流水同步
5. 对账结果
6. 政府监管查看

验收：

```text
物业生成账单
住户完成支付
银行流水入库
系统自动更新账单状态
政府端能看到缴费率、流水、异常预警
```

## 9. 验收用例

### 用例 1：物业公司注册

1. 物业提交注册资料
2. 平台管理员审核通过
3. 系统创建物业租户
4. 物业管理员登录
5. 物业暂无小区数据

### 用例 2：物业绑定小区

1. 物业申请服务某小区
2. 平台或政府审核通过
3. 生成 `PROPERTY_SERVICE` 关系
4. 物业能查看该小区房屋、账单、报修
5. 物业不能查看其他小区

### 用例 3：政府监管

1. 街道绑定多个小区 `JURISDICTION`
2. 街道账号登录
3. 只能查看辖区小区
4. 可查看监管报表和投诉
5. 不能修改物业收费标准，除非授权

### 用例 4：银行服务

1. 银行机构注册审核通过
2. 小区绑定银行代收服务
3. 物业生成账单
4. 住户支付
5. 银行回调写入流水
6. 物业和政府按权限查看对账

### 用例 5：住户多房屋

1. 住户实名注册
2. 绑定 A 小区 1101
3. 再绑定 B 小区 0802
4. 审核通过后可切换房屋
5. 每个房屋只显示对应账单、公告、投票

## 10. 风险和处理

### 风险 1：权限改造影响现有接口

处理：

先增加新表和新服务，旧字段保留一版。列表接口逐个迁移，避免一次性大改。

### 风险 2：注册审核流程过重

处理：

一期不要引入完整工作流引擎，使用 `registration_application` 和状态流转即可。二期再接 Flowable 或 Camunda。

### 风险 3：银行真实对接周期长

处理：

保留现有 `IntegrationService` dev 模式，先把接口契约、流水表、对账表跑通，再替换真实银行 SDK 或 HTTP 签名客户端。

### 风险 4：政府层级复杂

处理：

政府机构也作为 `tenant`，通过 `parent_id` 表示市、区、街道、社区层级，通过 `tenant_community_relation` 表示实际管辖范围。

## 11. 立即开工建议

下一步优先做这 6 个任务：

1. 新增 `V6__tenant_registration.sql`
2. 改造登录接口，返回用户可选租户
3. 新增 `AccessControlService`
4. 新增注册申请接口
5. 管理端新增“注册审核”菜单
6. 把 `/api/communities`、`/api/houses`、`/api/billing` 三类接口先接入新权限过滤

这 6 个完成后，系统就从“单小区角色演示”进入“多租户注册制平台”的实际落地点。
