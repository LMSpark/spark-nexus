# SPARK Nexus 开发说明

## 项目结构

- `backend/`：Spring Boot 3 后端，Java 17，Spring JDBC，Flyway，MySQL。
- `web-admin/`：Vue 3 管理端，Vite，TypeScript，Element Plus，ECharts。
- `owner-miniapp/`：uni-app 业主端，支持 H5 与微信小程序构建。
- `deploy/`：本地依赖编排、环境变量模板、数据库备份文件。
- `logs/`：本地启动日志输出目录。

## 本地依赖

建议使用项目自带 Docker Compose 启动 MySQL、Redis、MinIO：

```powershell
cd D:\物业管理
docker compose -f deploy\docker-compose.yml up -d mysql redis minio
docker inspect -f "{{.State.Health.Status}}" spark-nexus-property-mysql
```

MySQL 默认连接信息：

- 地址：`localhost:3806`
- 数据库：`property_revenue`
- 用户名：`property`
- 密码：`property123`
- root 密码：`root123456`

## 启动后端

```powershell
cd D:\物业管理\backend
mvn spring-boot:run
```

健康检查：

```powershell
Invoke-RestMethod http://localhost:8580/api/health
```

后端默认读取 `backend/src/main/resources/application.yml`，测试读取 `backend/src/test/resources/application.yml`。常用环境变量：

- `DB_URL`
- `DB_USERNAME`
- `DB_PASSWORD`
- `JWT_SECRET`
- `SENSITIVE_ENCRYPTION_KEY`
- `CORS_ALLOWED_ORIGINS`
- `INTEGRATION_MODE`

## 启动管理端

```powershell
cd D:\物业管理\web-admin
npm install
npm run dev
```

访问：`http://localhost:5683`

开发代理在 `web-admin/vite.config.ts` 中配置，默认把 `/api` 转发到 `http://localhost:8580`。

## 启动业主端 H5

```powershell
cd D:\物业管理\owner-miniapp
npm install
npm run dev:h5
```

访问：`http://localhost:5684`

业主端开发代理在 `owner-miniapp/vite.config.ts` 中配置。真机或线上联调时，建议设置 `VITE_API_BASE` 指向后端地址。

## 演示账号

密码均为 `admin123`：

- `admin`：系统管理员
- `gov`：政府监管员
- `street`：街道审核员
- `committee`：业委会
- `property`：物业人员
- `owner`：业主端账号

## 后端开发约定

主要代码位置：

- 启动类：`backend/src/main/java/com/dsyg/platform/PublicRevenuePlatformApplication.java`
- 鉴权：`backend/src/main/java/com/dsyg/platform/auth/`
- 业务接口：`backend/src/main/java/com/dsyg/platform/modules/`
- 统一异常：`backend/src/main/java/com/dsyg/platform/common/ApiExceptionHandler.java`
- 跨域和拦截器：`backend/src/main/java/com/dsyg/platform/WebConfig.java`
- 数据迁移：`backend/src/main/resources/db/migration/`

新增接口时建议：

1. 路径统一使用 `/api` 前缀。
2. 先在 Controller 中做角色校验，例如 `requireAnyRole(...)`。
3. 涉及小区数据时，通过 `AccessControlService` 校验数据范围。
4. 写操作需要记录审计日志或数据导出日志时，沿用现有 `audit(...)`、`logDataExport(...)` 模式。
5. 不要直接修改旧 Flyway 文件，新增迁移请按下一个版本号创建 `V41__xxx.sql`。

## 数据库开发

当前数据库由 Flyway 管理。新增表、字段、索引或初始化数据时：

```text
backend/src/main/resources/db/migration/V41__your_change_name.sql
```

迁移脚本建议：

- 表名、字段名使用小写下划线。
- 字符集保持 `utf8mb4`。
- 重要业务编号加唯一索引。
- 跨模块查询高频字段加普通索引。
- 已上线环境不要改历史迁移文件，避免 Flyway checksum 不一致。

导出当前数据库：

```powershell
cd D:\物业管理
$stamp = Get-Date -Format 'yyyyMMdd-HHmmss'
docker exec spark-nexus-property-mysql sh -lc "mysqldump -uroot -proot123456 --default-character-set=utf8mb4 --single-transaction --routines --events --triggers --hex-blob --set-gtid-purged=OFF --no-tablespaces --databases property_revenue --result-file=/tmp/property_revenue_full_$stamp.sql"
docker cp "spark-nexus-property-mysql:/tmp/property_revenue_full_$stamp.sql" "deploy\property_revenue_full_$stamp.sql"
```

恢复数据库见 `部署说明.md`。

## 管理端开发约定

主要代码位置：

- 页面入口：`web-admin/src/App.vue`
- API 封装：`web-admin/src/api.ts`
- 全局样式：`web-admin/src/styles.css`
- 构建配置：`web-admin/vite.config.ts`

开发注意：

- 所有后端请求优先使用 `api(...)` 或 `apiBlob(...)`。
- 需要登录态的请求会自动附带 `Authorization: Bearer <token>`。
- 新增菜单时同步检查 `roleNav` 和后端 `/api/auth/capabilities` 返回。
- 下载 CSV/PDF 使用 `apiBlob(...)`，避免按 JSON 解析二进制响应。
- 管理端是单文件主页面，改动前先搜索已有模块模式，尽量沿用现有表格、弹窗、抽屉写法。

## 业主端开发约定

主要代码位置：

- 页面入口：`owner-miniapp/src/pages/index/index.vue`
- 页面配置：`owner-miniapp/src/pages.json`
- 应用入口：`owner-miniapp/src/main.ts`
- 构建配置：`owner-miniapp/vite.config.ts`

开发注意：

- H5 本地开发可依赖 `/api` 代理。
- 小程序真机不走 Vite 代理，必须配置 `VITE_API_BASE` 或运行时 `apiBase`。
- 涉及房屋、投票、工单、缴费时，必须按后端返回的已审核房屋关系使用 `houseId`。
- 不要只依赖登录 token 的默认小区，业主可能绑定多套房屋。

## 测试与验证

后端测试：

```powershell
cd D:\物业管理\backend
mvn test
```

管理端构建：

```powershell
cd D:\物业管理\web-admin
npm run build
```

业主端 H5 构建：

```powershell
cd D:\物业管理\owner-miniapp
npm run build:h5
```

接口与代理检查：

```powershell
Invoke-RestMethod http://localhost:8580/api/health
Invoke-RestMethod http://localhost:5683/api/health
Invoke-RestMethod http://localhost:5684/api/health
```

## 常见问题

### 后端启动时报数据库连接失败

先确认 MySQL 容器是否健康：

```powershell
docker ps --filter "name=spark-nexus-property-mysql"
docker inspect -f "{{.State.Health.Status}}" spark-nexus-property-mysql
```

如果未启动：

```powershell
docker compose -f deploy\docker-compose.yml up -d mysql
```

### 前端页面请求 `/api` 失败

确认后端在 `8580`，再确认前端代理可用：

```powershell
Invoke-RestMethod http://localhost:8580/api/health
Invoke-RestMethod http://localhost:5683/api/health
```

如果部署到 Nginx，需要在 Nginx 中配置 `/api` 反向代理到后端。

### Flyway checksum 不一致

说明已经执行过的迁移文件被改动。开发阶段优先恢复数据库或新增下一版迁移，不建议修改历史迁移。

### 登录后菜单缺失

检查三处：

- 登录账号所属角色。
- `user_tenant_relation`、`data_authorization` 是否有 ACTIVE 授权。
- 后端 `/api/auth/capabilities` 返回的 `navKeys` 是否包含对应菜单。

## 提交前检查清单

- 后端：`mvn test`
- 管理端：`npm run build`
- 业主端：`npm run build:h5`
- 新接口已做角色和数据权限校验
- 新表或字段已写 Flyway 迁移
- 导出、回调、审批、支付等关键操作有审计或日志留痕
- 前端新增 API 路径能通过本地代理访问
